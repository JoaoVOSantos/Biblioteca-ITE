document.getElementById("mySelect").addEventListener("change", function() {

    // Quando o valor do select mudar, remova a borda preta
    this.style.border = "none"; // Remove a borda
    this.style.boxShadow = "none"; // Remove a sombra   
});
